<!DOCTYPE html>
<html>
<head>
	<title>Hello World!</title>
</head>
<body>
	<% for (var i = 0; i < posts.length; i++) { %>
		<div class="post">
			<h1>
				<a href="/post/<%= (i + 1)%>">
					<%= posts[i].title%>
				</a>
			</h1>
			<p><%= posts[i].content%></p>
		</div>
	<% } %>
</body>
</html>
